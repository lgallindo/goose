# Agent Sync Bus — Cross-Repo Coordination

**Created**: 2026-07-10T03:47:00Z  
**Repos**: `foss/goose` (consumer) ↔ `research/secret_local_llm` (runtime owner)

## Bus location

```
/home/lugatj/code/.agent_sync/
├── runtime_status.json   # llama-server health (flock-protected writes)
├── runtime_status.lock   # advisory lock file
└── task_claims.jsonl     # optional append-only task claims (future)
```

Override with `AGENT_SYNC_DIR`.

## Ownership

| Artifact | Writer | Reader |
|----------|--------|--------|
| `runtime_status.json` | secret_local_llm / `agent-bus-publish.sh` | goose / `agent-bus-wait.sh` |
| `SESSION_HANDOFF.md` (per repo) | both agents | both agents |
| `/home/lugatj/code/SESSION_HANDOFF.md` | both agents | both agents |

## Workflow

### secret_local_llm agent

```bash
just server   # background
/home/lugatj/code/foss/goose/scripts/agent-bus-publish.sh
```

### goose agent

```bash
/home/lugatj/code/foss/goose/scripts/agent-bus-wait.sh 60
./scripts/test-local-editor.sh
```

## Goose → local llama-server (verified 2026-07-10)

**Env-only** (recommended; no `~/.config/goose` edits):

```bash
OPENAI_HOST=http://127.0.0.1:38080 \
OPENAI_API_BASE=http://127.0.0.1:38080/v1 \
OPENAI_API_KEY=no-key-needed \
GOOSE_PROVIDER=openai \
GOOSE_MODEL=qwen2.5-coder-1.5b-instruct \
goose run --no-profile --text "ping"
```

**profiles.yaml note**: goose v1.40.0 has no `--profile` flag on `goose run` or `goose session start`.
`~/.config/goose/profiles.yaml` `secret-local` exists but `host` should be `http://127.0.0.1:38080/v1`
(operator edit). Env vars override for headless harnesses.

**Binary**: `~/.local/bin/goose` → debug build 1.40.0 (symlink).

## Harness status (2026-07-10)

| Harness | Result |
|---------|--------|
| env-only `goose run` ping | PASS (`HARNESS_PING`) |
| `scripts/test-local-editor.sh` | run after counter fix |
| `secret_local_llm` `just test-goose` | Test 1 PASS; Test 3 FAIL (toolshim/json — use `GOOSE_TOOLSHIM=true` or accept) |
